name: NVIDIA Free Endpoint Monitor

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Test GitHub Actions
        run: |
          echo "=================================="
          echo "NVIDIA Free Endpoint Monitor"
          echo "GitHub Actions is working!"
          echo "UTC time: $(date -u)"
          echo "=================================="
